---
title: Measure Types
description: Overview of available measure types in CAMS
icon: ruler-combined
---

This page contains an overview of all measure types available in sampling packages; both packages that are are built-in to the [carp_mobile_sensing](https://pub.dev/packages/carp_mobile_sensing) framework, as well as the external ones.
This list is constantly evolving as we release more and more support.

## Measure type format

The `dataType` property of a [`Measure`](https://pub.dev/documentation/carp_core/latest/common_carp_core_common/Measure-class.html) specify what type of data to collect.
The string version is typically a `namespace` + `name`. CAMS-native measure types have the namespace `dk.cachet.carp` and the name is the measure `type`:

```
dk.cachet.carp.<type>
```

The different types is specified in the tables below.

## Event-based vs. one-time measures

Most measures are **event-based** (EB), i.e., once triggered and started, they continuously sample data from their sensor until explicitly stopped. For example, collecting a step event every time a step is detected on the phone.
However, some measures are **one-time** (OT), i.e., sampling one piece of data when triggered. For example, collecting weather data from the Open Weather API.

It is important to know the type (EB vs. OT) of a measure since an event-based measure can be started and run until stopped, whereas a one-time measure needs to be triggered to sample data. This can, for example, be done periodically using appropriate triggers, like the [`PeriodicTrigger`](https://pub.dev/documentation/carp_mobile_sensing/latest/domain/PeriodicTrigger-class.html).  

The difference when specifying the study protocol is illustrated below.
The first task added to the protocol contains a one-time measure (device info), which is triggered only once.
The second task contains a set of event-based measures that are triggered to start immediately and keep sampling data based on events.
The third task collects weather (one-time measure) periodically every 5 minutes.

```dart
  // Collect device info only once, when this study is deployed.
  protocol.addTaskControl(
    OneTimeTrigger(),
    BackgroundTask(
        measures: [Measure(type: DeviceSamplingPackage.DEVICE_INFORMATION)]),
    phone,
  );

  // Immediately start collecting step count, ambient light, screen activity, and
  // battery level events. All event-based measures.
  protocol.addTaskControl(
    ImmediateTrigger(),
    BackgroundTask(measures: [
      Measure(type: SensorSamplingPackage.STEP_EVENT),
      Measure(type: SensorSamplingPackage.AMBIENT_LIGHT),
      Measure(type: DeviceSamplingPackage.SCREEN_EVENT),
      Measure(type: DeviceSamplingPackage.BATTERY_STATE),
    ]),
    phone,
  );

  // Add a background task that collects weather every 5 minutes.
  protocol.addTaskControl(
      PeriodicTrigger(period: Duration(minutes: 5)),
      BackgroundTask(
          measures: [Measure(type: ContextSamplingPackage.WEATHER)]),
      weatherService);
```

Each [`SamplingPackage`](https://pub.dev/documentation/carp_mobile_sensing/latest/runtime/SamplingPackage-class.html) provides a list of what types of measures it supports using the [`dataTypes`](https://pub.dartlang.org/documentation/carp_mobile_sensing/latest/runtime/SamplingPackage/dataTypes.html) property.
Each sampling package also comes with a [default sampling schema](https://pub.dev/documentation/carp_mobile_sensing/latest/runtime/SamplingPackage/samplingSchemes.html) for each of its measure types.

## Available measure types

CAMS comes with a large set of predefined measure types, each available in different sampling packages. Some measures are [part of CAMS](#built-in-sampling-packages), others are available in [separate sampling packages](#external-sampling-packages), and some are part of [device-specific sampling packages](#wearable-device-sampling-packages).

The tables below describe, for each measure:

* **Type Name** - all type names are of the format `dk.cachet.carp.<type>`, where `type` is listed below
* **Availability** on Android and/or iOS
* The **sampling package** containing the measure
* A **description**

<Note>
Measures marked with * are one-time measures.
</Note>

### Built-in sampling packages

Type         | Android/iOS | Package | Description                                                  |
:------------|:-----------:|:--------|------------------------------------------------------------- |
`acceleration` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Rate of change in velocity in x, y, z - including gravity |
`nongravitational acceleration` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Rate of change in velocity in x, y, z, - excluding gravity |
`acceleration features` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Acceleration events over a specific sampling period and calculates higher-order features |
`rotation` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Rotation of the device in x, y, z (typically measured by a gyroscope) |
`magneticfield` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Magnetic field around the device in x,y,z (typically measured by a magnetometer) |
`stepevent` | **+ +** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Step events from the device on-board sensor |
`ambientlight` | **+ -** | [sensors](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_sensors/) | Ambient light from the phone's front light sensor |
`deviceinformation`*| **+ +** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | Basic device information |
`application information`* | **+ +** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | Information about the app
`batterystate` | **+ +** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | Battery charging status and battery level |
`screenevent` | **+ -** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) |Screen events (on/off/unlock) |
`applifecycleevent` | **+ +** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | App lifecycle state events (inactive, hidden, paused, resumed, detached) |
`freememory` | **+ -** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | Free memory |
`timezone`* | **+ +** | [device](https://pub.dev/documentation/carp_mobile_sensing/latest/infrastructure_sampling_packages_device/) | Timezone of the device |

### External sampling packages

Type         | Android/iOS | Package | Description                                                          |
:------------|:-------:|:-------:|------------------------------------------------------------------------- |
`location` | **+ +** | [`context`](https://pub.dartlang.org/packages/carp_context_package) | Listens to the stream of location changes from the phone's OS |
`activity` | **+ +** | [`context`](https://pub.dartlang.org/packages/carp_context_package) | Activity as events from the OS's build-in activity recognition API |
`weather`*|    **+ +**     |    [`context`](https://pub.dartlang.org/packages/carp_context_package ) | Detailed  weather information for the current location of the phone |
`airquality`*     |    **+ +**     |    [`context`](https://pub.dartlang.org/packages/carp_context_package) | Air quality of the current location of the phone (from land-based stations) |
`geofence`     |    **+ +**     |    [`context`](https://pub.dartlang.org/packages/carp_context_package) | Tracking entry, dwell, and exit event in circular geofences |
`mobility`     |    **+ +**     |    [`context`](https://pub.dartlang.org/packages/carp_context_package) | Track location and calculates mobility features based on the [mobility_features](https://pub.dev/packages/mobility_features) plugin |
`bluetooth` | **+ +** | [`connectivity`](https://pub.dartlang.org/packages/carp_connectivity_package) | Scanning nearby BLE devices |
`wifi` | **+ -** | [`connectivity`](https://pub.dartlang.org/packages/carp_connectivity_package) | Collects SSID and BSSID from connected wifi networks |
`connectivity` | **+ +** | [`connectivity`](https://pub.dartlang.org/packages/carp_connectivity_package) | Connectivity status events |
`beacon` | **+ +** | [`connectivity`](https://pub.dartlang.org/packages/carp_connectivity_package) | Scans for nearby [iBeacons](https://en.wikipedia.org/wiki/IBeacon) |
`audio`*| **+ +** | [`media`](https://pub.dartlang.org/documentation/carp_media_package/latest/) | Records audio from the device microphone |
`noise` | **+ +** | [`media`](https://pub.dartlang.org/documentation/carp_media_package/latest/) | Detects ambient noise from the device microphone |
`apps`* | **+ -** | [`apps`](https://pub.dartlang.org/packages/carp_apps_package) | List of installed apps |
`appusage`* | **+ -** | [`apps`](https://pub.dartlang.org/packages/carp_apps_package) | List of app usage |
`phonelog`* | **+ -** | [`communication`](https://pub.dartlang.org/packages/carp_communication_package) | Log of phone calls in/out |
`textmessagelog`*| **+ -** | [`communication`](https://pub.dartlang.org/packages/carp_communication_package) | Log of text messages (sms) in/out |
`textmessage` | **+ -** | [`communication`](https://pub.dartlang.org/packages/carp_communication_package) | Text message (sms) events when received |
`calendar`* | **+ +** | [`communication`](https://pub.dartlang.org/packages/carp_communication_package) | Collects calendar events from the calendars on the phone |
`survey`*| **+ +** | [`survey`](https://pub.dev/packages/carp_survey_package) | Collects data from surveys (questionnaires) via the [Research Package](https://pub.dev/packages/research_package) and cognitive assessments from the [Cognition Package](https://pub.dev/packages/cognition_package) |

### Wearable device sampling packages

Type         | Android/iOS | Package |Description |
-------------|:-------:|:-----------:|:------------ |
`health` | **+ +** | [`health`](https://pub.dev/packages/carp_health_package) | Collects different types of health data from [Apple Health](https://www.apple.com/health/) and [Google Health Connect](https://health.google/products/) that may come from connected wearable devices |
`movisens`   | **+ -** | [`movisens`](https://pub.dev/packages/carp_movisens_package) | Activity and Heart Rate Monitoring using the [Movisens](https://www.movisens.com/en/) [Move4](https://www.movisens.com/en/products/activity-sensor/) and [EcgMove4](https://www.movisens.com/en/products/ecg-sensor/) devices |
`esense` | **+ +** | [`esense`](https://pub.dev/packages/carp_esense_package) | Inertial measurement unit (IMU) sensor events and button press/release events from the [eSense](http://www.esense.io) device |
`polar` | **+ +** | [`polar`](https://pub.dev/packages/carp_polar_package) | Inertial measurement (accelerometer, gyroscope, magnetometer) and heart rate (ECG, PPI, PPG, HR) data from the [Polar](http://www.polar.com) devices |
`movesense` | **+ +** | [`movesense`](https://pub.dev/packages/carp_movesense_package) | State (tap), heart rate (HR), ECG, and Inertial Movement Unit (IMU - accelerometer, gyroscope, magnetometer) data from the [Movesense](http://www.movesense.com) devices |

<Note>
Each of these device measures has sub-types, such as `dk.cachet.carp.movisens.hr` for heart rate (HR) measures from the Movisens device.
</Note>
